package com.github.cishiv.dominoes.types.board;

/**
 * Allows for customizable toString operations against tiles, letting us output an understandable board layout.
 */
public enum PrintOrientation {
    DEFAULT,
    INVERSE;
}
